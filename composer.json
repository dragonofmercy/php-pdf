{
    "name": "dragonofmercy/phppdf",
    "description": "Modern PHP library for PDF generation.",
    "type": "library",
    "license": "MIT",
    "keywords": ["pdf", "pdf-generation", "pdf-writer"],
    "require": {
        "php": "^8.4",
        "ext-mbstring": "*",
        "ext-openssl": "*",
        "ext-zlib": "*"
    },
    "require-dev": {
        "phpunit/phpunit": "^11.0",
        "phpstan/phpstan": "^2.0",
        "symfony/process": "^7.0"
    },
    "suggest": {
        "ext-imagick": "Decode WebP images (preferred backend)",
        "ext-gd": "Decode WebP images (fallback) and JPEGs inside an SVG <filter> region"
    },
    "autoload": {
        "psr-4": { "DragonOfMercy\\PhpPdf\\": "src/" }
    },
    "autoload-dev": {
        "psr-4": { "DragonOfMercy\\PhpPdf\\Tests\\": "tests/" }
    },
    "scripts": {
        "test": "phpunit --exclude-group conformance",
        "test:all": "phpunit",
        "test:conformance": "phpunit --group conformance",
        "analyse": "phpstan analyse",
        "check": ["@analyse", "@test"],
        "check:full": ["@analyse", "@test:all"]
    },
    "scripts-descriptions": {
        "test": "Run the test suite minus the slow veraPDF conformance group",
        "test:all": "Run every test including the veraPDF conformance group",
        "test:conformance": "Run only the veraPDF conformance group (needs the oracle)",
        "check": "analyse + fast tests (default gate)",
        "check:full": "analyse + every test including veraPDF conformance"
    },
    "config": {
        "sort-packages": true
    },
    "minimum-stability": "stable"
}
