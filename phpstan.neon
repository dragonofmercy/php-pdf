parameters:
    level: max
    paths:
        - src
        - tests
        - examples
    excludePaths:
        # Exclude Seafile sync-conflict duplicate files (sync artifacts, never committed)
        - '*SFConflict*'
    tmpDir: .phpstan.cache
